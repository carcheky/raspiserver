services:

# authelia  
  authelia:
    image: authelia/authelia
    container_name: authelia
    restart: always
    user: "1000:1000"
    environment:
      TZ: ${TIMEZONE}
    volumes:
      - ${RASPICONFIG}/authelia/config:/config